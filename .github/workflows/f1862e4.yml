name: Workspace
on:
    - push
jobs:
    Run:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version:
                    - 12.x
        steps:
            -
                uses: actions/checkout@v1
            -
                name: Run
                run: ./run.sh
